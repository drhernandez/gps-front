const TOGGLE_SIDEBAR = 'TOGGLE_SIDEBAR';

function toogleSidebarAction() {
  return {
    type: TOGGLE_SIDEBAR
  }
}

const types = {
  TOGGLE_SIDEBAR
};

const actions = {
  toogleSidebarAction
}

export { types, actions };